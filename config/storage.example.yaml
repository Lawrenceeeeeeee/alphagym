clickhouse:
  host: localhost
  port: 8123
  username: alphagym
  database: alphagym
  workspace: research
  secure: false
# Save as <root>/storage.yaml. Password: ALPHAGYM_CLICKHOUSE_PASSWORD.
# Preserve workspace when moving the root or restoring on another server.
